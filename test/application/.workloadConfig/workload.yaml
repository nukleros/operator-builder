name: webstore
kind: StandaloneWorkload
spec:
  domain: acme.com
  apiGroup: apps
  apiVersion: v1alpha1
  apiKind: WebStore
  clusterScoped: false
  companionCliRootcmd:
    name: webstorectl
    description: Manage webstore stuff like a boss
  resources:
  - app.yaml
