apiVersion: v1
kind: Namespace
metadata:
  #+docs: Defines the namespace in which all resources of this
  # component belong
  name: tenancy-system  # +operator-builder:field:name=namespace,default=tenancy-system,type=string
  labels:
    workload-collection: default-collection
