apiVersion: v1
kind: ConfigMap
metadata:
  label: 
    # +operator-builder:field:name=test,default=tescase-dev,type=string,replace="testcase-blah"
    app: testcase-blah-speaker
  name: contour-configmap
  namespace: ingress-system  # +operator-builder:field:name=namespace,default=ingress-system,type=string
data:
  # +operator-builder:field:name=test,default=myoption,type=string,replace="testcase-blah"
  config.yaml: |
    ---
    someoption: testcase
    anotheroption: testcase-blah
    justtesting: multistringyaml
---
apiVersion: v1
kind: Secret
metadata:
  name: contour-secret
  # +operator-builder:field:name=namespace,default=ingress-system,type=string
  namespace: ingress-system
  labels:
    workload-collection: default-collection  #+operator-builder:collection:field:name=collectionLabel,type=string
stringData:
  some: secretstuff
