kind: ComponentWorkload
name: component-missing-kind
spec:
  api:
    clusterScoped: false
    domain: acme.com
    group: apps
    version: v1alpha1
  companionCliSubcmd:
    description: Manage myapp workload
    name: myapp
  dependencies: []
  resources:
    - /path/to/my/component-workload-config.yaml
