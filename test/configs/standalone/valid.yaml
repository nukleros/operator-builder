kind: StandaloneWorkload
name: standalone-workload-config
spec:
  api:
    clusterScoped: false
    domain: acme.com
    group: apps
    kind: MyApp
    version: v1alpha1
  companionCliRootcmd:
    description: Manage myapp workload
    name: myapp
  resources:
    - /path/to/my/child-resources.yaml
