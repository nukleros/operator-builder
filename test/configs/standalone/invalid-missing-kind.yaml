kind: StandaloneWorkload
name: standalone-missing-kind
spec:
  api:
    clusterScoped: false
    domain: acme.com
    group: apps
    version: v1alpha1
  companionCliRootcmd:
    description: Manage myapp workload
    name: myapp
  resources:
    - /path/to/my/child-resources.yaml
