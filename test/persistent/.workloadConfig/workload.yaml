name: persistent
kind: StandaloneWorkload
spec:
  api:
    domain: acme.com
    group: apps
    version: v1alpha1
    kind: Persistent
    clusterScoped: false
  companionCliRootcmd:
    name: persistentctl
    description: Manage persistent stuff like a boss
  resources:
  - app.yaml
